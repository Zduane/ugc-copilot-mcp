# Block install/postinstall/prepare lifecycle scripts by default.
# Defense against npm supply-chain attacks like TanStack 2026-05-11.
# After `npm ci`/`npm install`, run:
#
#   npm rebuild esbuild
ignore-scripts=true
